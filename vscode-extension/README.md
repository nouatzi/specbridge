# SpecBridge VS Code Extension (Scaffold)

This folder contains a minimal VS Code extension scaffold that connects VS Code to SpecBridge via the `specbridge lsp` command.

Status: scaffold only (not wired into the root build).

